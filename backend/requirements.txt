# 與 pyproject.toml 同步維護；建議改用 uv sync（依 uv.lock 精確還原）。
# torch 從 PyTorch CPU index 取，避免抓到 CUDA 版。
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi
uvicorn[standard]
sqlalchemy
alembic
pydantic-settings
psycopg2-binary
pydantic[email]
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
python-multipart
pdfminer.six
pdfplumber
httpx
faiss-cpu
numpy
pymupdf
Pillow
slowapi
# 簡體→台灣正體（Apache 2.0；不用 GPLv2+ 的 zhconv）
opencc-python-reimplemented
# Windows CPU 可用組合（paddlepaddle 3.3.x 有 PIR regression，見 pyproject 註解）
paddleocr==3.4.1
paddlepaddle==3.2.2
paddlex[ocr]==3.4.3
# 預設 OCR 引擎（OCR_ENGINE=rapid）的 onnx 三件套
rapidocr
rapid-layout
rapid-table
onnxruntime
# Gemini 雲端 provider（現行官方 SDK）
google-genai
# RAG rerank：cross-encoder 重排（CPU）
torch
sentence-transformers
