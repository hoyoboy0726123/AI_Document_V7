# 本地 RAG 在小 VRAM 上的效能調校筆記

> 實測環境：NVIDIA RTX 5060 Laptop（8,151 MiB VRAM）、Ollama 0.32.3、Windows 11。
> 本文所有數字都是實際量到的，不是估算。適用於任何「LLM + 嵌入模型都跑在同一張卡」的本地 RAG 專案。

---

## TL;DR

**在小 VRAM 機器上，嵌入模型的大小比它的品質更重要。**

RAG 的每一次查詢都必須「先嵌入問句 → 再用 LLM 生成」。如果這兩個模型的執行期佔用加起來超過 VRAM，Ollama 就得在每次查詢時把其中一個換出、另一個換入 —— **換模成本會完全淹沒模型品質帶來的差異**。

把嵌入模型從 `qwen3-embedding:8b`（執行期 7.2 GB）換成 `bge-m3`（執行期 664 MB）後：

| 情境 | 換前 | 換後 | 改善 |
|---|---|---|---|
| LLM 常駐後做一次向量檢索 | 12,600 ms | **151 ms** | **83 倍** |
| 冷啟動檢索 | ~12 秒 | **99 ms** | ~120 倍 |
| 兩個模型能否同時常駐 | ❌ 不可能 | ✅ 可以 | — |

---

## 一、症狀

- 單次問答耗時 50 秒以上，但 GPU 使用率大部分時間很低
- `nvidia-smi` 看起來 VRAM 沒滿，卻感覺不到 GPU 加速
- 重複問同一個問題，第二次沒有明顯變快

這些症狀容易被誤判成「模型太大」或「context 設太高」，但真正的元凶通常是**模型換入換出**。

---

## 二、診斷方法（可直接複製到其他專案）

### 步驟 1：看模型有沒有溢位到 CPU

```bash
ollama ps
```

關鍵是 **PROCESSOR** 欄位：

```
NAME                  SIZE      PROCESSOR          CONTEXT
qwen3-embedding:8b    7.2 GB    14%/86% CPU/GPU    4096      <<< 溢位了
bge-m3:latest         664 MB    100% GPU           4096      <<< 完全進 GPU
```

只要不是 `100% GPU`，就代表有部分層跑在 CPU 上，速度會直接掉一個量級。

> ⚠️ 注意 `SIZE` 是**執行期佔用**（含 KV cache），不是 `ollama list` 顯示的磁碟大小。
> `qwen3-embedding:8b` 磁碟 4.7 GB，執行期卻要 7.2 GB —— 用磁碟大小估算 VRAM 會嚴重低估。

### 步驟 2：量換模成本（這一步最關鍵）

單獨量「檢索有多快」會被熱快取騙到。必須量「**LLM 佔住 VRAM 之後，再做一次檢索**」：

```bash
# A. 嵌入模型已在記憶體
time curl -s -X POST $API/vector-search/test -d '{"query":"...","top_k":3}'

# B. 強制載入 LLM，模擬回答階段
time curl -s -X POST http://127.0.0.1:11434/api/generate \
     -d '{"model":"<你的LLM>","prompt":"hi","stream":false}'

# C. 再檢索一次 —— 這次必須把 LLM 換出、嵌入模型換入
time curl -s -X POST $API/vector-search/test -d '{"query":"...","top_k":3}'
```

**C 減去 A 就是換模成本。** 實測：

| | 嵌入模型 7.2 GB | 嵌入模型 664 MB |
|---|---|---|
| A. 熱快取檢索 | 1.5 秒 | 99 ms |
| B. 載入 LLM | 19.6 秒 | 12.4 秒 |
| C. LLM 常駐後檢索 | **12.6 秒** | **151 ms** |
| **換模成本（C − A）** | **≈ 11 秒** | **≈ 0.05 秒** |

RAG 每問一題會換模兩次，所以光是載入就多花 20～30 秒。

### 步驟 3：確認兩個模型能否共存

```bash
ollama ps
nvidia-smi --query-gpu=memory.total,memory.used --format=csv,noheader
```

成功的樣子（兩個都在、都是 100% GPU、還有餘裕）：

```
qwen3:8b         5.6 GB    100% GPU
bge-m3:latest    664 MB    100% GPU
8151 MiB, 6194 MiB          <<< 用 6.2 GB，剩 1.9 GB
```

---

## 三、根因

VRAM 預算是**執行期佔用相加**，不是磁碟大小相加：

```
qwen3:8b（LLM）          5.6 GB
qwen3-embedding:8b       7.2 GB
                        -------
                        12.8 GB  >  8.15 GB   → 必定互相換出
```

換成小嵌入模型後：

```
qwen3:8b（LLM）          5.6 GB
bge-m3                   0.66 GB
                        -------
                         6.3 GB  <  8.15 GB   → 可同時常駐
```

---

## 四、解法

**挑一個「小到能與 LLM 共存」的嵌入模型，而不是挑最強的嵌入模型。**

| 模型 | 磁碟 | 執行期 | 維度 | 8GB 卡上可否與 LLM 共存 |
|---|---|---|---|---|
| `qwen3-embedding:8b` | 4.7 GB | 7.2 GB | 4096 | ❌ |
| `bge-m3` | 1.2 GB | 0.66 GB | 1024 | ✅ |
| `quentinz/bge-large-zh-v1.5` | 651 MB | ~0.5 GB | 1024 | ✅ |

`bge-m3` 額外的好處：它是多語模型，對「**英文文件 + 中文提問**」這種跨語言檢索通常明顯優於單語的 `bge-large-zh`。若你的語料正好是這種組合，等於速度和品質雙贏。

### 不要做的事

- ❌ **不要靠調低 `num_ctx` 來擠 VRAM**。見下方第六節，代價遠比想像大。
- ❌ **不要設 `OLLAMA_KEEP_ALIVE=0`**。它會在每次請求後立刻卸載模型，批次 ingest 時等於每個 chunk 都重載一次模型。維持 `5m` 讓 Ollama 自己用 LRU 決定換出誰，反而更快。

---

## 五、副發現一：嵌入端沒傳 `num_ctx`，模型會用預設值載入

很多專案的 Ollama client 只在 `chat` / `generate` 帶 `options`，**`embed` 端點忘了帶**。結果嵌入模型永遠用 Ollama 的預設 context 載入，KV cache 白白多佔 VRAM。

實測同一個模型、同一段輸入：

| 呼叫方式 | CONTEXT | SIZE | PROCESSOR |
|---|---|---|---|
| 不帶 options（多數專案的現況） | 4096 | 7.2 GB | **14%/86% CPU/GPU** |
| 帶 `options.num_ctx=2048` | 2048 | 6.3 GB | **100% GPU** |

**只是傳了 `num_ctx` 就省下 0.9 GB，剛好從溢位變成完全進 GPU。** 回傳的向量維度與內容完全正確。

### 檢查方式

```python
# 找你的 embed 呼叫，看 payload 裡有沒有 options
payload = {
    "model": model,
    "input": inputs,
    "keep_alive": keep_alive,
    "truncate": True,
    # "options": {"num_ctx": ...},   <<< 少了這行
}
```

### 建議的修法

不要寫死，從「單段輸入的字元上限」推導：

```python
# 中文最壞情況約 1 token/字，取 2 的冪次向上取整
max_chars = settings.EMBEDDING_MAX_CHARS      # 例如 2000
payload["options"] = {"num_ctx": 2048}
```

> ⚠️ 設太小會**截斷**輸入而不是報錯，被截掉的內容將永遠檢索不到。
> 務必讓 `num_ctx ≥ 單段最長輸入的 token 數`。

---

## 六、副發現二：`num_ctx` 調高到某個點之後完全沒用

很多專案會把 `num_ctx` 設得很大（例如 24576），以為「context 越大檢索脈絡越多」。實測**不一定**——因為多數 RAG 實作另有一個「送進 prompt 的字元預算上限」，那才是真正的天花板。

本專案實測（`RAG_CONTEXT_BUDGET_CHARS = 20000` 是硬上限）：

| `num_ctx` | 實際可用的檢索脈絡 |
|---|---|
| 4096 | 2,192 字元 |
| 6144 | 10,384 字元 |
| **8192** | **18,576 字元** ← 已達上限的 93% |
| 12288 | 20,000 字元（觸頂） |
| 24576 | 20,000 字元（**完全沒有多拿到任何東西**） |

**結論**：
- 8192 → 24576 只是白白多吃 VRAM，一個字都沒多拿
- 8192 → 4096 卻是斷崖，脈絡只剩 12%，答案完整度會嚴重受損

**在調 `num_ctx` 之前，先找出你專案裡那個字元/token 預算上限在哪。** 兩者要一起看，單獨調任一邊都會得到錯誤結論。

---

## 七、代價與注意事項

### 換嵌入模型 = 向量維度改變 = 必須全量重建

`qwen3-embedding:8b` 是 4096 維，`bge-m3` 是 1024 維。舊向量與新索引維度不符，直接加入會報錯。

**正確順序（順序錯了會白做）**：

1. 改設定（模型名稱 + 單段輸入字元上限）
2. **重啟服務**（設定多在啟動時載入）
3. **清空所有向量**（會刪索引檔並重置記憶體快取）
4. **逐份文件重新嵌入**

> ⚠️ **只跑「重建索引」是無效的。** 那類功能是從資料庫既有的向量重建索引檔，
> 而那些向量還是舊維度，跑一百次都一樣。必須真的重新呼叫模型做嵌入。

### 相似度門檻要重新校準

不同嵌入模型的相似度分數尺度不同。原本針對舊模型調的 `MIN_SIMILARITY_SCORE`（例如 0.3）換模後可能過鬆或過嚴，**務必重新量過**。

### 有評測就先跑評測

任何檢索層的改動都可能有反直覺的結果。若專案有回歸評測，換模前後各跑一次再決定保留。

---

## 八、檢查清單

給其他專案照做的順序：

- [ ] `ollama ps` 確認每個模型的 PROCESSOR 是不是 `100% GPU`
- [ ] 用 A/B/C 三步量出換模成本（C − A）
- [ ] 把「LLM 執行期 + 嵌入模型執行期」相加，跟 VRAM 比對
- [ ] 若超出：**優先換小的嵌入模型**，而不是調 `num_ctx` 或 `keep_alive`
- [ ] 檢查 `embed` 呼叫有沒有帶 `options.num_ctx`
- [ ] 找出專案裡的脈絡字元預算上限，確認 `num_ctx` 沒有設在無效區間
- [ ] 換模後：清空向量 → 全量重新嵌入 → 重新校準相似度門檻

---

## 附錄：本次的完整實測數據

| 項目 | `qwen3-embedding:8b` | `bge-m3` |
|---|---|---|
| 磁碟大小 | 4.7 GB | 1.2 GB |
| 執行期佔用 | 7.2 GB | 664 MB |
| PROCESSOR | 14%/86% CPU/GPU | 100% GPU |
| 向量維度 | 4096 | 1024 |
| 冷啟動檢索 | ~12 秒 | 99 ms |
| LLM 常駐後檢索 | 12,600 ms | 151 ms |
| 能否與 5.6 GB 的 LLM 共存 | ❌ | ✅（合計 6.3 / 8.15 GB） |

VL 視覺解析 16 頁 PDF 的 ingest 時間也從 **593 秒降到 259 秒**。
（此項有干擾因素：同時也調整了提示詞，所以不能完全歸因於換模，但 VL 與嵌入模型不再互相換出應是主因。）
